# Optional. Without a key, /api/chat returns a deterministic catalog-grounded fallback.
# Never commit a real API key. Configure secrets in the deployment environment.
ANTHROPIC_API_KEY=

# Keep the model configurable so it can be changed without a code deploy.
CHAT_MODEL=claude-haiku-4-5
