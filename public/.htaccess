Options -MultiViews
RewriteEngine On

# Keep real Vite assets/files untouched.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SPA fallback for direct loads such as /ukazka/mylo.
RewriteRule ^ index.html [L]
